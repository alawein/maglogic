# Copy to docker/.env and replace every value before starting containers.
# Generate secrets locally, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JUPYTER_TOKEN=
POSTGRES_PASSWORD=
